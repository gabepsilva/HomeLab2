---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jkclaim
  namespace: jenkins
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
