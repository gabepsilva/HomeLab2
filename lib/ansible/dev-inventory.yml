---
all:
  children:

    k8s:
      children:

        loadbalancers:
          hosts:
            loadbalancer1:
              ansible_host: loadbalancer1.dev.psilva.org
              
        nodes:
          children:

            masters:
              hosts:
                master1:
                  ansible_host: master1.dev.psilva.org

            workers:
              hosts:
                node1:
                  ansible_host: node1.dev.psilva.org   
                node2:
                  ansible_host: node2.dev.psilva.org 
      vars:
        ansible_ssh_user: kube

    vault:
      hosts:

        vault1:
            ansible_host: vault.dev.psilva.org
      vars:
        ansible_ssh_user: vault
